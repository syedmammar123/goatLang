arr = [2,3,4,5]

fun insertIntoArray(arr, idx, val){
    i = arr.length - 1
    until (i >= idx){
        a = arr->(i + 1)
        a = arr->(i)
    }
    b = arr->(idx)
    b = val
    display(arr,idx,val)
}
