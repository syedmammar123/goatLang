arr = [2,3,4,5]

fun insertIntoArray(arr, idx, val){
    i = arr.length - 1
    until (i >= idx){
        arr->(i + 1) = arr->(i)
    }
    arr->(idx) = val
    display(arr,idx,val)
}

insertIntoArray(arr, 2, 45)
